import Navbar from '@/components/shared/Navbar';
import React from 'react';

const AuthLayout = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default AuthLayout;